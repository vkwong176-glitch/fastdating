rules_version = '2';
// Fast Dating｜Firestore 安全規則
// ───────────────────────────────────────────────────────────────
// 與 App 內 [FirestorePaths.users] 一致：會員文件在集合 **「users」**，
// 文件 ID 為 **Firebase Authentication 的 UID**（非 Console 自建之其它集合，
// 例如獨立的「UID／test_user_001」並非本 App 會員入口）。
//
// [UserFirestoreService.ensureUserProfile]／[seedPublicProfileIfMissing] 對
// **`users/{目前登入者的 uid}`** 做 merge/set；規則必須在 **已有 Auth** 時允許該路徑寫入。
//
// ── 現行策略 ──
// 下方 `/{document=**}` 對 **任一已登入使用者**（`request.auth != null`）放行**讀、寫**，
// 因此已包含：**可 set 自己的 `users/{yourUid}`**，以及探索頁讀他人 `users` 等現有行為。
//
// ⚠️ 若 Firebase Console「規則」與本檔不同，Android 可能被拒寫而入失敗。**請將本檔部署上雲後再測**：
//   firebase deploy --only firestore:rules
//
// 正式上線前應視需求改為「依集合分拆授權」，勿長期全站對所有登入者可寫任何路徑。
service cloud.firestore {
  match /databases/{database}/documents {

    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
